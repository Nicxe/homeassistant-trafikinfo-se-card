name: 🐞 Bug Report
description: Report a problem with Trafikinfo SE 
title: "[Bug]: "
labels: ["bug"]
assignees: ["Nicxe"]
body:
  - type: checkboxes
    id: prerequisites
    attributes:
      label: Prerequisites
      options:
        - label: I am running the latest version of Trafikinfo SE
          required: true
        - label: I have searched existing issues
          required: true
        - label: I have restarted Home Assistant
          required: true

  - type: input
    id: version
    attributes:
      label: Trafikinfo SE Version
      placeholder: e.g. 1.0.0
    validations:
      required: true

  - type: textarea
    id: current_behavior
    attributes:
      label: Current Behavior
      description: What is happening?
    validations:
      required: true

  - type: textarea
    id: expected_behavior
    attributes:
      label: Expected Behavior
      description: What should happen?
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ### Enable Debug Logging for Trafikinfo SE

        Before submitting a bug report, please enable debug logging:

        1. Go to **Settings → Devices & Services → Integrations**
        2. Find **Trafikinfo SE** and click the three dots → **Enable Debug Logging**
        3. Restart Home Assistant
        4. Reproduce the issue
        5. Go to **Settings → System → Logs**
        6. Download the full log or copy the relevant part for Trafikinfo SE
        Paste the debug log below.

  - type: textarea
    id: logs
    attributes:
      label: Logs
      description: Relevant Home Assistant logs
      render: shell
